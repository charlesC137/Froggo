import { LimitPostDivsPipe } from './limit-post-divs.pipe';

describe('LimitPostDivsPipe', () => {
  it('create an instance', () => {
    const pipe = new LimitPostDivsPipe();
    expect(pipe).toBeTruthy();
  });
});
